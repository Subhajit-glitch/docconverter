/** Placeholder: add an OCR provider (for example, a managed OCR API) here for scanned PDFs. Keep credentials in environment variables and send files only to an approved provider. */
export async function extractTextWithOcr(_inputPath: string): Promise<string> { throw new Error("OCR is not configured."); }
