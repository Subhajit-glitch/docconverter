export type DocumentKind = "pdf" | "docx";
const pdfMime = "application/pdf"; const docxMime = "application/vnd.openxmlformats-officedocument.wordprocessingml.document";
export function identifyDocument(name: string, mime: string, bytes: Buffer): DocumentKind { const extension = name.toLowerCase().split(".").pop(); if (extension === "pdf" && (mime === pdfMime || !mime) && bytes.subarray(0, 5).toString() === "%PDF-") { if (bytes.includes(Buffer.from("/Encrypt"))) throw new Error("Password-protected PDFs are not supported."); if (!bytes.includes(Buffer.from("%%EOF"))) throw new Error("This PDF appears corrupted or incomplete."); return "pdf"; } if (extension === "docx" && (mime === docxMime || !mime) && bytes.subarray(0, 4).equals(Buffer.from([0x50,0x4b,0x03,0x04]))) return "docx"; throw new Error("The file type, MIME type, or signature is not a supported PDF or DOCX."); }
