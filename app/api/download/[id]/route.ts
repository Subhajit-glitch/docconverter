import { readFile } from "node:fs/promises"; import { NextResponse } from "next/server"; import { getJob } from "@/lib/storage";
export const runtime = "nodejs";
export async function GET(_: Request, { params }: { params: Promise<{ id: string }> }) { const { id } = await params; const job = getJob(id); if (!job?.outputPath || !job.downloadName) return NextResponse.json({ error: "This download is unavailable or has expired." }, { status: 404 }); try { const data = await readFile(job.outputPath); return new NextResponse(data, { headers: { "Content-Type": job.downloadName.endsWith(".pdf") ? "application/pdf" : "application/vnd.openxmlformats-officedocument.wordprocessingml.document", "Content-Disposition": `attachment; filename="${job.downloadName}"`, "Cache-Control": "no-store", "X-Content-Type-Options": "nosniff" } }); } catch { return NextResponse.json({ error: "This download is unavailable." }, { status: 404 }); } }
